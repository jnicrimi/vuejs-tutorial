{
  "scripts": {
    "prepare": "husky install"
  },
  "lint-staged": {
    "**/*.md": "markdownlint --ignore node_modules --ignore app/README.md --ignore app/node_modules .",
    "**/*.{yaml,yml,yamllint}": "yamllint --strict ."
  },
  "devDependencies": {
    "@commitlint/cli": "^18.4.1",
    "@commitlint/config-conventional": "^18.0.0",
    "husky": "^8.0.3",
    "lint-staged": "^15.0.2"
  }
}
